# Use an offical WordPress image as the base image
FROM wordpress:latest

# Your custom configurations and settings can be added here
# For example, you might add additional plugins or theme files

# Expose port 80 for HTTP
EXPOSE 80
